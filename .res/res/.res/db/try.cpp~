#include <iostream>

using namespace std;
int fileSize(){
    ifstream mySource;
    mySource.open("img.txt", ios_base::binary);
    mySource.seekg(0,ios_base::end);
    int size = mySource.tellg();
    mySource.close();
    return size;
}

main(){
	cout<<fileSize();
}
